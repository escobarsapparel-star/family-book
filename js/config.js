// Family Book external service configuration.
// Add a Google Maps Platform browser key here when ready.
// IMPORTANT: Restrict the key to your Family Book web domain/referrers.
window.FB_CONFIG = Object.assign({
  googleMapsApiKey: "",
  // Optional ISO-3166 region code such as "za". Leave blank for worldwide suggestions.
  googleMapsRegion: ""
}, window.FB_CONFIG || {});
